# Dependencies (restored via a package manager, never committed)
node_modules/
vendor/

# Build output (added if/when we introduce a build step)
dist/
*.css.map

# Secrets & environment — NEVER in git history
.env
.env.*
*.local
wp-config.php

# OS & editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Logs
*.log
